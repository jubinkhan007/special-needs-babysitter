name: domain
description: Domain layer with entities, repository contracts, and usecases. Pure Dart - no Flutter imports.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  equatable: ^2.0.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
