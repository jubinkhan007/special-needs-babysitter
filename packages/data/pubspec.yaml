name: data
description: Data layer with DTOs, mappers, and repository implementations.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  networking:
    path: ../networking
  core:
    path: ../core
  dio: ^5.4.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0
  shared_preferences: ^2.5.4

  path: any
  mime: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.11.1
  freezed: ^3.1.0
  json_serializable: ^6.13.0
